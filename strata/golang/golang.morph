name: golang
kind: chunk
build-commands:
# Skip tests, which fail in containment
- cd src && TMPDIR=/tmp bash ./make.bash
install-commands:
- mkdir -p "$DESTDIR$PREFIX/bin/"
- install -m 755 bin/* "$DESTDIR$PREFIX/bin/"
