name: libelf
kind: chunk
build-system: autotools
configure-commands:
- autoreconf -i
- ./configure --enable-maintainer-mode
build-commands:
- cd libelf && make
install-commands:
- cd libelf && make install
